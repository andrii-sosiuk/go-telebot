# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  registry: ghcr.io
  repository: andrii-sosiuk/go-telebot
  tag: v3.0.0-464d723
  arch: amd64
  os: linux
  pullPolicy: IfNotPresent
secret:
  name: kbot
  tokenKey: token
  tokenName: TELE_TOKEN
